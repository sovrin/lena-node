export {
    BaseDictionaryProvider,
    DEFAULT_USER_AGENT,
    normalizeText,
    toAbsoluteUrl,
    withTimeout,
} from './base';

export type {
    DictionaryEntry,
    DictionarySection,
    DictionaryTerm,
} from './base';

export type { DictionaryProvider } from './interface';
